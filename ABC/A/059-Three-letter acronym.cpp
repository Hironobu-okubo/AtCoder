#include <bits/stdc++.h>
using namespace std;
 
int main(){
  string a,b,c;
  cin >> a >> b >> c;
  string ini = "";
  ini += toupper(a[0]);
  ini += toupper(b[0]);
  ini += toupper(c[0]);
  cout << ini <<endl;
}